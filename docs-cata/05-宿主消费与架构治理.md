# 宿主消费与架构治理

当前官方基线为 `rust-v0.153.4`。本文描述已落地的职责和消费链；固定差异统计见[差异索引](04-官方基线差异索引.md)，版本适配证据见[升级记录](06-0.153.4升级验证.md)。

## 1. Java 架构师可以怎样理解

| Rust 对象 | 类比与实际责任 |
| --- | --- |
| crate / Cargo path dependency | 接近 Maven 多模块源码依赖；链接进宿主二进制，不是启动完整官方 CLI |
| `ThreadManagerRuntimeOptions` | 显式依赖注入装配对象；保存进程提供的能力，再传入 thread/session |
| `Arc<dyn Trait>` | 共享的接口实现引用；需要关注并发、生命周期与取消，不能当作独立业务服务 |
| `RuntimeExtension` / Skill provider | 工具及 Skill 接入接口；产品实现归 Runtime，注册和执行边界归 Codex |
| `AppServerProcessOverrides` | App Server 装配层；除 ThreadManager 外还承接 RPC 与进程 MCP 配置 |
| `Session` / `LiveThread` | 上游执行与持久化生命周期 owner；宿主不应复制另一套会话真相 |

Runtime 的直接 Codex crate 消费清单由各 `crates/*/Cargo.toml` 生成，见[差异索引](04-官方基线差异索引.md)。`codex-app-server-client` 是 App Server 的开发测试依赖；生产 TUI 依赖保留在 `catalyst-codex`。

路径依赖会编译被消费的库代码，但不会自动运行依赖 crate 自身的测试。宿主测试通过与 fork 测试通过是两类证据。

## 2. 两条生产装配线

```text
Catalyst ProductRuntime
  ├─ catalyst-app-server::process_overrides
  │    → AppServerProcessOverrides → run_app_server_serve
  │    → ConfigManager / ThreadManager / RPC registry → Session
  └─ catalyst-codex::main
       → ThreadManagerRuntimeOptions + TuiRuntimeOptions
       → run_main_with_runtime_options → TUI / Session
```

以下 Runtime 路径均相对 Runtime 仓库根；fork 路径相对本仓库根。

| 能力 | App Server | TUI | 产品实现 owner |
| --- | --- | --- | --- |
| required instructions、HTTP、catalog、model policy | 注入 | 注入 | Runtime `crates/catalyst-codex/src/runtime.rs`、`model_catalog.rs` |
| search、market data、image generation、private Skill extensions/provider | 注入 | 注入 | Runtime 对应 `catalyst-tool-*`、`catalyst-private-skills` |
| Browser extension | 条件注入 | 当前 main 未注入 | Runtime Browser 能力 |
| thread activity、title generator、产品 RPC | 注入 | 当前 main 未注入 | Runtime app-server；title model/usage 归 Runtime |
| credential/readiness/onboarding UI | RPC 业务路径 | `CatalystTuiModelRuntime` | Runtime `tui_model_runtime.rs` |
| process MCP table replacement | 通过 flatten 的共享 CLI 接受 | 当前 TUI 装配未使用该入口 | fork CLI/config；调用者提供 server 表 |

精确入口：Runtime `crates/catalyst-app-server/src/lib.rs::process_overrides`、`src/main.rs`、`src/cli.rs`；`crates/catalyst-codex/src/main.rs`。共享 `ProductRuntime` 不代表最终工具集合相同。

## 3. 补丁能力账本

“默认变化”表示即便没有显式 override，fork 仍可能与官方不同。每一行是能力族，不代表单个提交或独立文件。

| 能力族 | 相对官方增加/改变什么 | 作用域及边界 | 合并重点 |
| --- | --- | --- | --- |
| 宿主 HTTP transport | `HttpTransportHandle`、Core client override；有 override 时不用模型 WebSocket | process 注入、session 消费；Provider 适配和凭据归 Runtime | Core client、session spawn、HTTP factory |
| catalog 与 model policy | 静态目录；HTTP 增量、图片迁移、请求 byte budget、并行工具能力覆盖 | 逐 model 策略；上游 `StaticModelsManager` 本身可复用，不是新造 catalog 引擎 | 新模型字段、模型切换、默认策略 |
| required instructions / prompt debug | 保留宿主基础指令并组合任务指令；使用同一 runtime options 生成 prompt snapshot | thread 创建；debug 不证明工具真实执行或权限闭环 | ThreadManager、prompt builder、恢复入口 |
| Runtime 工具扩展 | search/image gate、工具注册、受限图片输出提示 | extension 接口通用；Tavily、行情、图片服务实现均在 Runtime | `tools/spec_plan.rs`、extension registry；提示长度上限 |
| Skill provider | 按 authority/kind 路由，provider catalog/read 接入 | 显式 provider 注入；解密/授权不进入 fork | sources/catalog/tools 与宿主 entitlement |
| Skill roots 限制 | 不再自动发现用户及项目 `.agents/skills`；配置层、plugin、extra roots 仍保留 | **默认变化**，不由 runtime options 开关控制 | 上游 discovery 更新；不能整体恢复旧扫描逻辑 |
| RPC/native gateway | namespaced 产品方法；复用 native plugin list/turn start | process 注册；initialize、transport context、thread queue/session gate | `message_processor.rs`、plugin processors |
| process MCP 配置 | `--process-mcp-server` 替换指定 server 整表；其他 server 保留，rebuild 继续应用 | process config 接口；不是 ThreadManager option，也不是全局文件修改 | precedence、相对路径、refresh/rebuild、原配置约束 |
| 自动标题及事件 | 可注入 generator；持久化成功后通知；手工标题优先 | store 中异步任务；Core session 自建 metadata mutation gate | `LiveThread`、shutdown、`ThreadNameUpdated` 协议适配 |
| 流式 item 归属 | SSE 保留 item ID，交错 item 的 delta 按 ID 路由；提前文本 delta 缓冲 | **默认变化**，作用于响应流；无 ID 仍有 active-item 兼容路径 | SSE → ResponseEvent → sampling → 对外 item event |
| TUI 产品接入 | catalog picker、credential secret 输入、onboarding/readiness | 抽象 `TuiModelRuntime`；产品 Provider 业务在 Runtime | startup、event dispatch、submission、snapshots |
| HTTP client / 构建适配 | login 暴露 raw reqwest client；依赖/穷举 event match 随之变化 | API 适配与编译输入；不是独立产品功能 | custom CA/client policy、Cargo/Bazel 双构建一致性 |

Windows private desktop 在当前 0.153.4 中已无相对上游的 `windows-sandbox-rs` 差异，按上游平台实现维护，不再计为保留的 Catalyst patch；仍需真实 Windows 执行资格。

Core 的 byte budget 是历史大小估算与 4/5 阈值触发预采样压缩，不是精确序列化 body 上限。图片重编码和 Provider 协议转换仍在 Runtime；fork 图片迁移当前仅处理 `FunctionCallOutput`，不能泛称所有 tool output 都支持。

## 4. 已落地的架构边界

- Core 工具规划消费独立搜索能力声明，保留注册成功、feature、mode 和原生 gate；历史工具 wire 名称保持兼容，不用机械重命名替代语义设计。
- `client/catalyst/` 保存模型策略契约和请求变换纯函数；原 ModelClient 保留 baseline、增量计算和失败恢复。Runtime 负责 Provider 策略和最终 wire 转换。
- `app-server/src/catalyst/` 集中 RPC 契约及直接测试。initialize 门禁、原生 Turn/plugin gateway、配置加载继续由原 owner 执行，不增加第二套生命周期。
- `tui/src/catalyst/` 集中宿主模型与凭据展示契约；Runtime 的 `tui_model_runtime` 负责领域结果映射，测试独立成文件。
- `codex-utils-sensitive-string` 提供敏感值基础类型。Provider、credentials、providers、Anthropic adapter、tool-search 均无传递 TUI 依赖；App Server 仍经组合 crate `catalyst-codex` 到达 TUI。

目录、公开导出和来源识别见[模块边界](07-模块边界与代码识别.md)，敏感值语义见[敏感值分层](08-敏感值分层与Cata测试设计.md)。Workbench 不参与这些内部模块拆分；未来公开 DTO、事件、历史或发布契约变化仍需核对宿主消费者。

### 本地 Skill 发现与资源边界

原生 `skills/list` 的本地发现由 Fork loader 与文件系统 walk 负责；Runtime 公共技能安装和私有
Skill Provider 不替代该扫描器。walk 的 4 MiB 预算约束返回的路径清单，不是技能包或资源文件的大小上限。
技能发现显式选择所需文件名，仍遍历目录并保留条目、深度、目录数和软链策略；资源保持原路径可读。
其他 walk 调用默认不启用过滤。旧执行端可能忽略新字段，调用方仍须处理未过滤或截断的结果。

扫描警告进入既有 Skill errors，成功项继续返回；Workbench 负责对客完整性提示，不透传原始错误路径。
接口、兼容与测试入口见 [Patch 地图](01-Patch地图.md)。Fork 修改需进入重新构建的 Runtime 产物后才生效；
源码依赖可编译不等于已发布消费端取得修复。

### 持久事实、投影与恢复边界

SQLite 是存储格式，不是“均可删除的缓存”的分类。当前本地存储边界见
[Thread Store](../codex-rs/thread-store/README.md)；应按数据来源判断可否重建：

日常兼容矩阵按各库当前 schema、上一已知 schema 与更早已有迁移链建立；rollout 等非 SQLite
数据按存储格式及读写语义建立，不套用桌面发布版本。合成历史样本如实记录生成方法及语义
断言；真实旧 writer 的发布产物身份另用于跨发布或安装升级资格，见[同步手册](02-上游同步手册.md)。

| 数据 | 持久职责与恢复要求 |
| --- | --- |
| rollout JSONL 及其压缩形式 | 原生 canonical history，承载模型上下文与历史事件；恢复时保留对应 lineage 和原生关联身份 |
| state SQLite 中的用户组织与产品记录 | thread sections、projects/roots、项目幂等键、thread artifacts 等有独立写入事实；不能假设全部能由消息回放重建 |
| state SQLite 中的 thread 元数据 | 部分由历史观察更新，显式修改由 `ThreadStore::update_thread_metadata` 管理并维护部分旧格式兼容；逐字段证明来源，不能把整个数据库等同索引 |
| thread-history SQLite 中的历史投影、offset/ordinal checkpoint | 从 durable rollout materialize 的读取结构；只有源历史完整、当前 reader 可解释且重建入口已验证时，才具备相应重建资格 |
| 日志数据库 | 有预算和保留期限的诊断证据；不拥有用户历史，缺失不能靠执行历史完整恢复 |
| HTTP baseline、连接和 Provider response ID | 请求优化的运行态；不构成用户历史的持久权威 |

独立 SQLite 事实的例子见 [projects migration](../codex-rs/state/migrations/0049_projects.sql)、
[artifact migration](../codex-rs/state/migrations/0051_thread_artifacts.sql)；投影写入及 checkpoint
一致性见 [thread_history](../codex-rs/thread-store/src/local/thread_history.rs)。备份恢复必须按
Workbench `docs/400-contracts/430-customer-data-and-upgrades.md` 的完整恢复单位执行；删除某模块
后重建只恢复已经证明可重建的部分，不是恢复升级前数据的替代办法。

HTTP `previous_response_id` 仅用于 Provider 仍保有对应响应且本地 baseline 可安全复用的增量
请求。Core 的 [ModelClient](../codex-rs/core/src/client.rs) 持有 baseline、判断增量和处理失败；
Runtime 决定 Provider 策略与最终 wire。对已识别的 response ID 过期/不存在错误，当前 HTTP
路径回退发送完整上下文；[错误识别](../codex-rs/core/src/client/catalyst/host_http.rs)有具体条件，
不能扩大为所有错误的自动恢复承诺。重新恢复历史不以旧 response ID 继续可用为前提，远端
response ID、prompt cache 或旧连接也不能替代本地完整历史；崩溃前未落盘内容不由此补回。

## 5. 验证路由与证据边界

| 变化 | 直接行为证据 | 不可扩大解释为 |
| --- | --- | --- |
| 请求与图片 | Fork incremental HTTP 用例；Runtime `synthetic_policy_to_core_fallback_matrix` 的 17 个合成场景 | 真实 Provider、大图重编码或所有失败恢复 |
| RPC 隔离 | Fork registry 冲突/路由、initialize/context/error 分发；Runtime 原生 Turn 入口组合用例 | 所有 queue/cancel、真实 transport 认证 |
| process MCP | CLI 整表解析、ConfigManager refresh/reload、requirements 与 typed validation | 实际 MCP 子进程热替换全组合 |
| TUI 与敏感值 | readiness/onboarding/credentials、旧模型结果晚到、基础类型脱敏与 Runtime 适配 | 全部 GUI 交互或真实账号验证 |
| 装配与历史 | Runtime `runtime_composition`，TUI 入口装配 | 跨版本 N−1/N、未归档进程崩溃恢复 |
| 默认行为与平台 | Skill roots、流式 item、Windows desktop 的对应直接测试 | 空 overrides 等价官方或 macOS 代替 Windows |

升级后的执行记录集中在 [06](06-0.153.4升级验证.md) 和 [08](08-敏感值分层与Cata测试设计.md)，不将旧版本的通过结果计入当前资格。每次变更按[同步手册](02-上游同步手册.md)选择最低充分验证；完成这些治理不等于全仓或发布验收。

## 6. 异常反馈与诊断消费

用户提示默认说明失败影响和下一步；本地 Agent 先读短摘要，再按关联标识读取必要证据。三仓沿用现有 owner，不增加第二套错误协议或诊断生命周期。

| Owner | 责任与消费边界 |
| --- | --- |
| Workbench | 桌面用户提示、下一步入口、本地诊断摘要与技术支持包；对所选证据做脱敏投影，控制输出与导出预算、注明缺失或截断，并管理宿主产物保留 |
| Runtime | Provider、凭据及产品 RPC 的领域错误分类、可操作文案和安全诊断字段；TUI 适配提供 `ModelRuntimeError` 的 `code/message/action` |
| Fork / 上游 | 原生执行、重试、Turn 状态与证据存储；保持错误分类及关联标识，宿主通过现有接口消费，不在 fork 加入产品支持流程 |

App Server 的 `TurnError` 已区分 `message/codexErrorInfo/additionalDetails`，错误通知还提供 `willRetry/threadId/turnId`；自动重试通知不能直接解释为最终失败。宿主 RPC 错误通过 `JSONRPCErrorError` 的 `code/message/data` 原样返回，产品 handler 负责返回可安全消费的内容。接口与直接透传测试见 [RPC 契约](../codex-rs/app-server/src/catalyst/rpc_extension.rs)、[分发测试](../codex-rs/app-server/src/catalyst/message_processor_rpc_tests.rs)和 [TUI 错误契约](../codex-rs/tui/src/catalyst/model_runtime.rs)。

本地排查按 thread、turn、请求或 process 标识缩小范围，再读取原生事件和日志；短摘要保留原因分类、重试状态及证据入口。`query_logs` 的 `limit` 是可选参数，调用方必须明确选择有限范围；`query_feedback_logs_for_threads` 合并指定 threads 与其各自最近 process 的无 thread 日志，最终输出总计不超过 10 MiB。该读取能力不意味着支持包默认收集原生数据库、完整历史或所有日志；prompt、tool trace 和原始 Provider body 不默认外发，按需证据仍须经过宿主筛选与脱敏。

原生日志预算与保留事实以 [state 常量](../codex-rs/state/src/runtime.rs)、[日志存取](../codex-rs/state/src/runtime/logs.rs)及 [写入队列](../codex-rs/state/src/log_db.rs)为准：

- 每个 thread 分区、每个无 thread 的 process 分区分别保留至多约 10 MiB 可读日志内容和 1000 条记录，插入事务内裁剪。它们不是整个数据库的物理空间上限，也不是支持包预算。
- 启动维护清理 10 天前的日志；这不是持续执行的实时 TTL，不能据此承诺任意时刻只保留 10 天数据。
- 默认队列容量 2048、批量 512、刷新间隔 10 秒；队列满时丢弃新记录并计入 telemetry。保留裁剪、未刷新、队列丢失或读取限制都可能造成证据缺失，缺日志不能证明错误未发生。

Core 的请求 byte budget 只用于模型历史估算与压缩，不替代上述诊断输出、支持包和保留预算。变更宿主错误消费时核对原生重试语义与 RPC 透传；只有这些边界发生代码变化才运行对应 focused test，纯文档更新检查链接和差异。

## 7. 后续维护原则

每次上游更新先评估等价能力，能够恢复原生实现时删除重复 patch。只有受影响的契约、消费者或测试出现证据缺口时继续修改；不以持续拆文件、增加前缀或重复测试作为架构完成标准。历史阶段计划与过时统计保留在 Git 历史，当前文档只维护有效边界和验证入口。
